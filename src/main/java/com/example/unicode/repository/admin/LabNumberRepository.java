package com.example.unicode.repository.admin;

import com.example.unicode.entity.admin.Lab;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LabNumberRepository extends JpaRepository<Lab, Long> {
}
